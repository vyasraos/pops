name: "POPS CLI CodeQL Configuration"

disable-default-queries: false

queries:
  - uses: security-and-quality
  - uses: security-extended

paths-ignore:
  - "dist/**"
  - "node_modules/**"
  - "**/*.test.ts"
  - "**/*.test.js"
  - "tests/**"
  - "docs/**"

paths:
  - "src/**"

query-filters:
  - exclude:
      id: js/unused-local-variable
  - exclude:
      id: js/debugger-statement